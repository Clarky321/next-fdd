/*
Этот файл — основная конфигурация для проекта на Next.js.
*/

/** @type {import('next').NextConfig} */
const nextConfig = {
    // Здесь можно добавлять дополнительные настройки (например, redirects, rewrites)
};

export default nextConfig;
