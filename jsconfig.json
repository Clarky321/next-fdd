/* 
Этот файл используется для настройки путей и автодополнения в VS Code.
*/

{
  "compilerOptions": {
    "paths": {
      "@/*": ["./*"] // Упрощает импорт файлов с помощью "@/..." вместо относительных путей
    }
  }
}
